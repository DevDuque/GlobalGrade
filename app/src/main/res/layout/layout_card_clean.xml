<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/card_content"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="20dp"
    android:background="?attr/theme_cardBackground"
    android:layout_marginVertical="20dp"
    >

    <TextView
        android:id="@+id/course_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:textColor="?attr/theme_backgroundColor"
        android:fontFamily="@font/bai_jamjuree"
        android:textAlignment="center" />

    <TextView
        android:id="@+id/course_grade"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textColor="?attr/theme_backgroundColor"
        android:fontFamily="@font/bai_jamjuree"
        android:textAlignment="center" />

    <ImageView
        android:id="@+id/course_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/str_grade_complete"
        android:layout_gravity="center"
        android:paddingTop="20dp"
        android:src="?attr/theme_icon_help"
        />

</LinearLayout>
